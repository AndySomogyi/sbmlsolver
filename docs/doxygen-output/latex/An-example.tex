\hypertarget{An-example}{}\doxysection{An}
Add a function from the standard C library to the IR Module.

Add a function from the standard C library to the IR Module. example declaration is\+: declare double @pow(double, double)

the declaration is resolved with the standard C library. 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{using} powFn = double (*)(\textcolor{keywordtype}{double} x, \textcolor{keywordtype}{double} y);}
\DoxyCodeLine{powFn pow = (powFn) executionEngine-\/>getPointerToNamedFunction(\textcolor{stringliteral}{"{}pow"{}});}
\DoxyCodeLine{std::cout << pow(4, 2) << std::endl; \textcolor{comment}{// outputs 16}}

\end{DoxyCode}



\begin{DoxyCodeInclude}{0}

\end{DoxyCodeInclude}
 